{{- if and (.Values.cfgService.enabled) (not .Values.cfgService.config.database.useExisting) }}
---
apiVersion: v1

kind: PersistentVolumeClaim

metadata:
  name: {{ .Release.Name }}-cfg-db-claim0

spec:
  storageClassName: {{ .Values.cfgService.config.database.persistence.storageClass }}
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.cfgService.config.database.persistence.size }}
---
{{- end }}